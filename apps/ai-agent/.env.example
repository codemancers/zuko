OPENAI_API_KEY=

# Zuko backend URL — must match backend's BACKEND_URL
ZUKO_BACKEND_URL=http://localhost:3001

# TUI dev only — copy better-auth.session_token from browser DevTools → Application → Cookies
# Not needed when using HTTP API (session cookie forwarded automatically)
ZUKO_SESSION_TOKEN=
