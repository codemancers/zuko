# Ignore everything by default
**/*

# Include only what's needed for Docker build
!dist/**
!prisma/schema.prisma
!prisma/schema/**

# Exclude node_modules even if in dist (will be installed fresh)
dist/node_modules
